name: uailm
description: Unified AI Life Manager
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # HTTP requests to your FastAPI backend
  http: ^1.2.0

  # State management
  provider: ^6.1.2

  # Local storage
  shared_preferences: ^2.2.3

  # Icons
  cupertino_icons: ^1.0.6
  flutter_local_notifications: ^17.0.0 # ADD THIS
  timezone: ^0.9.4
  device_calendar: ^4.3.2
  google_sign_in: ^7.2.0
  googleapis: ^16.0.0
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

google_sign_in: ^6.2.1
googleapis: ^13.2.0
flutter_secure_storage: ^9.0.0
